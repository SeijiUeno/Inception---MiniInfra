# Inception - MiniInfra
A mini infraestructure implemmentation with docker and docker-compose

## Project Structure
```
inception/
├── Makefile
├── docker-compose.yml
└── services/
    ├── nginx/
    │   └── Dockerfile
    ├── wordpress/
    │   └── Dockerfile
    └── mariadb/
        └── Dockerfile
```
